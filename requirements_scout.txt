# ═══════════════════════════════════════════════════════════
# Causal-Ensemble Paper Scout — Python Dependencies
#
# NO torch. NO sentence-transformers. NO heavy ML.
# Estimated Render RAM usage: ~150 MB (well within 512 MB free tier)
#
# RULE: numpy and scipy must NOT be pinned to exact old versions.
# numpy==1.26.4 and scipy==1.13.1 have no pre-built wheel for
# Python 3.12 (Render default). pip compiles from source → hangs.
# Using >= lets pip pick the latest wheel that installs instantly.
#
# Install locally:
#     pip install -r requirements_scout.txt
# ═══════════════════════════════════════════════════════════

# ── Data & Math ─────────────────────────────────────────────
pandas==2.2.2
numpy>=1.26.4        # unpinned — lets pip use pre-built wheel for Python 3.12
yfinance==0.2.40
scipy>=1.13.1        # unpinned — same reason as numpy above

# ── HTTP — curl_cffi required by modern yfinance ─────────────
# yfinance >= 0.2.40 uses curl_cffi internally for Yahoo requests.
# Also used by ScraperAPI tier to pass proxy with correct TLS fingerprint.
curl_cffi==0.7.4

# ── Database ────────────────────────────────────────────────
sqlalchemy==2.0.31
psycopg2-binary==2.9.9
pgvector==0.3.2

# ── Logging ─────────────────────────────────────────────────
structlog==24.2.0

# ── Environment ─────────────────────────────────────────────
python-dotenv==1.0.1

# ── HTTP / Network ───────────────────────────────────────────
requests==2.32.3
urllib3==2.2.2

# ── Misc yfinance dependencies ───────────────────────────────
multitasking==0.0.11
frozendict==2.4.4
peewee==3.17.6
beautifulsoup4==4.12.3
html5lib==1.1
lxml==5.2.2