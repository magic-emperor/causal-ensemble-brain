# ═══════════════════════════════════════════════════════════
# Causal-Ensemble Paper Scout — Python Dependencies
#
# NO torch. NO sentence-transformers. NO heavy ML.
# Estimated Render RAM usage: ~150 MB (well within 512 MB free tier)
#
# Install locally:
#     pip install -r requirements_scout.txt
# ═══════════════════════════════════════════════════════════

# ── Data & Math ─────────────────────────────────────────────
# numpy and scipy MUST be pinned to versions with pre-built wheels
# for Python 3.11 AND 3.12. Older pins caused source compilation hang.
numpy==2.2.4
scipy==1.15.2
pandas==2.2.2
yfinance==0.2.40

# ── HTTP — curl_cffi required by modern yfinance ─────────────
curl_cffi==0.7.4

# ── Database ────────────────────────────────────────────────
sqlalchemy==2.0.31
psycopg2-binary==2.9.9
pgvector==0.3.2

# ── Logging ─────────────────────────────────────────────────
structlog==24.2.0

# ── Environment ─────────────────────────────────────────────
python-dotenv==1.0.1

# ── HTTP / Network ───────────────────────────────────────────
requests==2.32.3
urllib3==2.2.2

# ── Misc yfinance dependencies ───────────────────────────────
multitasking==0.0.11
frozendict==2.4.4
peewee==3.17.6
beautifulsoup4==4.12.3
html5lib==1.1
lxml==5.2.2